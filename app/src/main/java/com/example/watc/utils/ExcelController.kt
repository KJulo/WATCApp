package com.example.watc.adapters

/*
import android.util.Log
import com.example.watc.models.IncidenciasList
import com.example.watc.ui.ListaIncidencias
import org.apache.poi.ss.usermodel.Cell
import org.apache.poi.ss.usermodel.Row
import org.apache.poi.xssf.usermodel.XSSFWorkbook
import java.io.FileNotFoundException
import java.io.FileOutputStream
import java.io.IOException
*/

class ExcelAdapter () {
    /*incidencias: IncidenciasList
    public fun crearExcel(){
        val wb = XSSFWorkbook()
        val sheet = wb.createSheet("Incidencias")
        val rowNum : Int = 1
        val rowCol : Int = 1
        val i: Int = 0

        val row : Row = sheet.createRow(0)
        var cell : Cell = row.createCell(0)
        cell.setCellValue("Descripción")
        val cell1 : Cell = row.createCell(1)
        cell1.setCellValue("Fecha")
        val cell2: Cell = row.createCell(2)
        cell2.setCellValue("¿Está resuelta?")
        val cell3: Cell = row.createCell(3)
        cell3.setCellValue("Tipo")
        val cell4: Cell = row.createCell(4)
        cell4.setCellValue("Ubicación")
        try{
            var output : FileOutputStream = FileOutputStream("Incidencias.xls")
            wb.write(output)
            wb.close()
        }catch(e: FileNotFoundException){
            Log.e("Excel","Archivo no encontrado")
        }catch(e: IOException){
            Log.e("Excel","No se pudo guardar archivo")
        }

    }
     */


}